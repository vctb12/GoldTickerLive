'use strict';

// Compatibility entrypoint retained for existing server/tests while DataCore v2 keeps its
// metal-neutral contract in a focused module.
module.exports = require('./datacore-observations');
