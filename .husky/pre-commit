#!/usr/bin/env sh

# Husky v9 pre-commit hook.
# Runs lint-staged on staged files only. Full test suite runs in CI, not here,
# to keep local commits fast and unblock work-in-progress branches.
npx --no-install lint-staged
